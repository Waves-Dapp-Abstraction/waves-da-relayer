PORT=3000
NODE_URL=https://nodes-testnet.wavesnodes.com
CHAIN_ID=84

REGISTRY_ADDRESS=3MpHSUmakaCCcQkwATctWuChM6QkX3dBWAr
RELAYER_SEED=

# Dev: optional. Production mainnet: use .env.mainnet.example (PRODUCTION=true, 32+ char secret)
JWT_SECRET=your-super-secret-key-change-in-production

# Optional — Redis for auth challenges (required for multi-instance production)
# REDIS_URL=redis://127.0.0.1:6379

# Optional — comma-separated origins; unset or * = allow all (dev only)
# CORS_ORIGINS=http://localhost:5173,http://localhost:5174

# PRODUCTION=true enforces JWT_SECRET (32+) and CORS_ORIGINS (not *)

FEE_REGULAR=500000
FEE_VERIFIER=900000
REFUND_GUARD_ENABLED=true

RATE_LIMIT_MAX=100
RATE_LIMIT_WINDOW_MS=60000

DAPP_CONFIG_PATH=./dappConfig.json
